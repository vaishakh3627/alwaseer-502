import React from "react";
import { Card } from "react-bootstrap";

function UserList() {
  return (
    <Card style={{ height: "100vh", backgroundColor: "red" }}>
      <Card.Header>User List</Card.Header>
    </Card>
  );
}

export default UserList;
